import { createMuiTheme } from '@material-ui/core';

const theme = createMuiTheme({
  palette: {
    background: {
      default: '#F0F0F7'
    },
    primary: {
      main: '#3B86FF'
    },
    secondary: {
      main: '#43435C'
    },
    text: {
      primary: '#595B67',
      secondary: '#7C7E89'
    },
    white: '#FFFFFF'
  }
});

export default theme;
